<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Maalamall"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/map_key"/>
        <uses-library android:name="org.apache.http.legacy"
            android:required="false"/>

        <activity
            android:name=".MapsActivity"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.FinalProject" />
        <activity
            android:name=".Register"
            android:theme="@style/Theme.FinalProject" />
        <activity android:name=".PostAd" />
        <activity android:name=".Category" />
        <activity android:name=".Category2" />
        <activity android:name=".Category3" />
        <activity android:name=".Category4" />
        <activity android:name=".Category5" />
        <activity android:name=".MyAccount" />
        <activity android:name=".ContactUs" />
        <activity android:name=".Buyer" />
        <activity android:name=".ViewAd" />
        <activity android:name=".ViewAd2" />
        <activity android:name=".ViewAd3" />
        <activity android:name=".ViewAd4" />
        <activity android:name=".Email" />
        <activity android:name=".ViewAd5" />
        <activity android:name=".Categories" />
        <activity android:name=".ContactSeller" />
        <activity android:name=".EditProfile" />
    </application>

</manifest>